import { ColumnHeaderPipe } from './column-header.pipe';

describe('ColumnHeaderPipe', () => {
  it('create an instance', () => {
    const pipe = new ColumnHeaderPipe();
    expect(pipe).toBeTruthy();
  });
});
